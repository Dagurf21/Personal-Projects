// Template string
`This is a template string`

// Mulitline strings
let text = `This is a multiline
string that
just works!`;

console.log(text);

let language = "Javascript";
let template_string = `I am learning ${language}`;
console.log(template_string)
